export enum GameStoreName {
  GOG = "gog",
  EPIC = "epic",
  AMAZON = "amazon",
  DEFAULT = "default",
}

export enum GameStoreContext {
  LIBRARY = "library",
  DETAILS = "details",
  HOME = "home",
  SEARCH = "search",
}
